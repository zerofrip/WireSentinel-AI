name: CI

on:
  push:
    branches: [main, master]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Checkout WireSentinel (shared-types)
        uses: actions/checkout@v4
        with:
          repository: WireSentinel/WireSentinel
          path: WireSentinel

      - name: Checkout WireSentinel-XDR (PlaybookKind)
        uses: actions/checkout@v4
        with:
          repository: WireSentinel/WireSentinel-XDR
          path: WireSentinel-XDR

      - name: Checkout WireSentinel-CNAPP (reference)
        uses: actions/checkout@v4
        with:
          repository: WireSentinel/WireSentinel-CNAPP
          path: WireSentinel-CNAPP

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: WireSentinel-AI

      - name: Run workspace tests
        working-directory: WireSentinel-AI
        run: cargo test --workspace
